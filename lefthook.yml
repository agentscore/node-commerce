pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "**/*.{ts,tsx,mjs}"
      run: bunx eslint --max-warnings 0 {staged_files}

pre-push:
  commands:
    typecheck:
      run: bun run build && bun run typecheck
    knip:
      run: bun run knip
